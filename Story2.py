from Getter import *

def story2(debug= False):
	if debug: print "--In story1 function--"
	
	friend1 = getWord("A Name: ". debug)
    body of water = getWord(A body of water: ". debug)
    object = getWord (an object): ". debug    
	out = ""
	out += "One day me and my friend, " + friend1
	out += "we were swimming in a, " + body of water1
    out += "we swam into a, " + object1
	return out
